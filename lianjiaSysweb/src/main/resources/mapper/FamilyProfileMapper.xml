<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lianjia.mapper.FamilyProfileMapper">

    <resultMap id="BaseResultMap" type="com.lianjia.entity.FamilyProfile">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="family_name" property="familyName"/>
        <result column="address_id" property="addressId"/>
        <result column="house_area" property="houseArea"/>
        <result column="house_type" property="houseType"/>
        <result column="decorate_date" property="decorateDate"/>
        <result column="last_service_date" property="lastServiceDate"/>
        <result column="next_service_date" property="nextServiceDate"/>
        <result column="has_reminder" property="hasReminder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, family_name, address_id, house_area, house_type, decorate_date, 
        last_service_date, next_service_date, has_reminder, status, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM family_profile
        WHERE id = #{id} AND status = 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM family_profile
        WHERE user_id = #{userId} AND status = 1
        ORDER BY create_time DESC
    </select>

    <select id="selectDefaultByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM family_profile
        WHERE user_id = #{userId} AND status = 1
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM family_profile
        WHERE 1=1
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="familyName != null and familyName != ''">
            AND family_name LIKE CONCAT('%', #{familyName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectNeedReminder" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM family_profile
        WHERE status = 1 AND has_reminder = 0
        ORDER BY next_service_date ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO family_profile (user_id, family_name, address_id, house_area, 
            house_type, decorate_date, last_service_date, next_service_date, has_reminder, status)
        VALUES (#{userId}, #{familyName}, #{addressId}, #{houseArea}, 
            #{houseType}, #{decorateDate}, #{lastServiceDate}, #{nextServiceDate}, #{hasReminder}, #{status})
    </insert>

    <update id="update">
        UPDATE family_profile
        SET
            <if test="familyName != null">family_name = #{familyName},</if>
            <if test="addressId != null">address_id = #{addressId},</if>
            <if test="houseArea != null">house_area = #{houseArea},</if>
            <if test="houseType != null">house_type = #{houseType},</if>
            <if test="decorateDate != null">decorate_date = #{decorateDate},</if>
            <if test="lastServiceDate != null">last_service_date = #{lastServiceDate},</if>
            <if test="nextServiceDate != null">next_service_date = #{nextServiceDate},</if>
            <if test="hasReminder != null">has_reminder = #{hasReminder},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        UPDATE family_profile SET status = 0 WHERE id = #{id}
    </delete>

    <update id="updateHasReminder">
        UPDATE family_profile SET has_reminder = #{hasReminder}, update_time = CURRENT_TIMESTAMP WHERE id = #{id}
    </update>

</mapper>
