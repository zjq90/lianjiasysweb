<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lianjia.mapper.UserAddressMapper">

    <resultMap id="BaseResultMap" type="com.lianjia.entity.UserAddress">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="phone" property="phone"/>
        <result column="province" property="province"/>
        <result column="city" property="city"/>
        <result column="district" property="district"/>
        <result column="detail_address" property="detailAddress"/>
        <result column="house_area" property="houseArea"/>
        <result column="house_type" property="houseType"/>
        <result column="is_default" property="isDefault"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, name, phone, province, city, district, detail_address, 
        house_area, house_type, is_default, status, create_time, update_time
    </sql>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_address
        WHERE id = #{id} AND status = 1
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_address
        WHERE user_id = #{userId} AND status = 1
        ORDER BY is_default DESC, create_time DESC
    </select>

    <select id="selectDefaultAddress" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_address
        WHERE user_id = #{userId} AND status = 1 AND is_default = 1
        LIMIT 1
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM user_address
        WHERE 1=1
        <if test="userId != null">
            AND user_id = #{userId}
        </if>
        <if test="name != null and name != ''">
            AND name LIKE CONCAT('%', #{name}, '%')
        </if>
        <if test="phone != null and phone != ''">
            AND phone = #{phone}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user_address (user_id, name, phone, province, city, district, 
            detail_address, house_area, house_type, is_default, status)
        VALUES (#{userId}, #{name}, #{phone}, #{province}, #{city}, #{district}, 
            #{detailAddress}, #{houseArea}, #{houseType}, #{isDefault}, #{status})
    </insert>

    <update id="update">
        UPDATE user_address
        SET
            <if test="name != null">name = #{name},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="province != null">province = #{province},</if>
            <if test="city != null">city = #{city},</if>
            <if test="district != null">district = #{district},</if>
            <if test="detailAddress != null">detail_address = #{detailAddress},</if>
            <if test="houseArea != null">house_area = #{houseArea},</if>
            <if test="houseType != null">house_type = #{houseType},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        UPDATE user_address SET status = 0 WHERE id = #{id}
    </delete>

    <update id="updateNonDefault">
        UPDATE user_address SET is_default = 0, update_time = CURRENT_TIMESTAMP 
        WHERE user_id = #{userId}
    </update>

    <update id="setDefault">
        UPDATE user_address SET is_default = 1, update_time = CURRENT_TIMESTAMP 
        WHERE id = #{id}
    </update>

</mapper>
